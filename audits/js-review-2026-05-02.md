# /js-review report — 2026-05-02

**Target:** last commit — `feat(pipeline): add NOAA X-ray flux as third signal (Phase 2C)`
**Commit:** `0633f92`
**Branch:** `feature/xray-flux`
**Files reviewed:** 6
**Lenses loaded:** javascript-pro, typescript-pro, code-reviewer
**React lens:** omitted — no JSX/TSX files in commit

---

## Files reviewed

| File | Lines | Type |
|------|-------|------|
| `app/services/fetchers/noaa-swpc.server.ts` | 41 | Fetcher (server-only) |
| `app/services/normalizers/noaa-swpc.ts` | 296 | Normalizer (pure transform) |
| `app/services/normalizers/noaa-swpc.test.ts` | 450 | Tests |
| `app/services/ingest/noaa-xray-flux.server.ts` | 77 | Ingest coordinator |
| `app/services/ingest/noaa-xray-flux.server.test.ts` | 163 | Tests |
| `scripts/ingest-noaa-xray-flux.ts` | 42 | CLI script |

---

## P0 — must fix (0 findings)

None.

---

## P1 — should fix (0 findings)

None.

---

## P2 — consider (0 findings)

None.

---

## Summary

All six files are clean across correctness, type safety, error handling, and test quality.

**`noaa-swpc.server.ts`**
Three symmetric fetch functions. Each returns `Promise<unknown>` — correct boundary for the fetcher layer. All use `response.ok` with a descriptive error message. No duplication worth abstracting (the URLs differ, the error messages differ; a generic helper would obscure intent for no gain).

**`noaa-swpc.ts` — `normalizeXRayFlux`**
`as Record<string, unknown>` cast is guarded by an explicit `typeof entry !== "object" || entry === null` check immediately above, with a comment explaining the safety guarantee. The `energyToSignalName()` private helper is the correct abstraction: one place to add or rename channels if NOAA extends the endpoint, and it throws on unknown strings rather than returning a silent fallback. All optional metadata fields (`observed_flux`, `electron_correction`, `electron_contaminaton`) are added conditionally with type and finite-checks rather than assumed present — correct for an external API.

**`noaa-xray-flux.server.ts`**
`import type { IngestResult }` (line 24) and `export type { IngestResult }` (line 25) both necessary — TypeScript re-exports do not introduce the type into local scope, so the separate import is required for the function's return type annotation. Not a redundancy. The `let records = []` + try/catch pattern is correct: the catch block always returns early, so `records` is guaranteed to be fully populated before the for-loop is reached. The clarifying comment on `signal: "xray-flux-long"` (lines 36–39) correctly documents the approximation without overpromising.

**`noaa-xray-flux.server.test.ts`**
`beforeEach(() => { db = openDb(":memory:"); })` guarantees full test isolation. `fakeFetcher` is a pure factory with no mutable captured state. The partial-overlap test correctly uses `RAW_TWO_TIMESTAMPS` (4 entries = 2 channels × 2 timestamps) to verify that dedup works per `{ timestamp, source, signal }` triplet, not per timestamp alone.

**`scripts/ingest-noaa-xray-flux.ts`**
`main().catch((err: unknown) => ...)` with `err instanceof Error ? err.message : String(err)` is the correct typed catch for a CLI entry point. `process.exit(1)` on error ensures CI pipelines detect failures.

---

## Clean files

All six files reviewed have zero findings.

- ✅ `app/services/fetchers/noaa-swpc.server.ts`
- ✅ `app/services/normalizers/noaa-swpc.ts`
- ✅ `app/services/normalizers/noaa-swpc.test.ts`
- ✅ `app/services/ingest/noaa-xray-flux.server.ts`
- ✅ `app/services/ingest/noaa-xray-flux.server.test.ts`
- ✅ `scripts/ingest-noaa-xray-flux.ts`
