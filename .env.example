# Session secret — generate with: openssl rand -base64 32
SESSION_SECRET=change-me-in-production

# SQLite database path (default: data/helios.sqlite relative to project root)
# DATABASE_PATH=/absolute/path/to/helios.sqlite

# Node environment
NODE_ENV=production
